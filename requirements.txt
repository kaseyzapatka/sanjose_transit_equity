# Minimal direct dependencies for the Diridon capacity + equity analysis.
# (Use environment.yml with conda for a fully pinned spatial stack.)
# Python 3.13
geopandas>=1.0
pandas>=2.0
numpy>=2.0
pyarrow>=15
shapely>=2.0
mapclassify>=2.6
requests>=2.31
pygris>=0.1
matplotlib>=3.8
folium>=0.16
